<ComplicationSlot slotId="${COMPLICATION_ID}" x="${POS_X}" y="${POS_Y}" width="${WIDTH}" height="${HEIGHT}" supportedTypes="EMPTY SHORT_TEXT LONG_TEXT RANGED_VALUE MONOCHROMATIC_IMAGE SMALL_IMAGE">
    <BoundingOval x="0" y="0" width="${WIDTH}" height="${HEIGHT}" outlinePadding="2" />
    <Complication type="MONOCHROMATIC_IMAGE">
        <Group name="monochromatic_oval" x="0" y="0" width="${WIDTH}" height="${HEIGHT}">
            <PartDraw x="0" y="0" width="${WIDTH}" height="${HEIGHT}" alpha="0">
                <Variant mode="AMBIENT" target="alpha" value="255" />
                <Arc direction="CLOCKWISE" startAngle="0" endAngle="360" centerX="${CENTER_X}" centerY="${CENTER_Y}" width="${RING_WIDTH}" height="${RING_HEIGHT}">
                    <Stroke color="${AMBIENT_COLOR}" cap="ROUND" thickness="2" />
                </Arc>
            </PartDraw>
            <PartDraw x="0" y="0" width="${WIDTH}" height="${HEIGHT}" alpha="255">
                <Variant mode="AMBIENT" target="alpha" value="0" />
                <Arc direction="CLOCKWISE" startAngle="0" endAngle="360" centerX="${CENTER_X}" centerY="${CENTER_Y}" width="${RING_WIDTH}" height="${RING_HEIGHT}">
                    <Stroke color="${COLOR}" cap="ROUND" thickness="2" />
                </Arc>
            </PartDraw>
        </Group>
        <Condition>
            <Expressions>
                <Expression name="monochromatic_ambientImage">
                    <![CDATA[
                        [COMPLICATION.MONOCHROMATIC_IMAGE] != null && [COMPLICATION.MONOCHROMATIC_IMAGE_AMBIENT] != null
                    ]]>
                </Expression>
                <Expression name="monochromatic_noAmbientImage">
                    <![CDATA[[COMPLICATION.MONOCHROMATIC_IMAGE] != null && [COMPLICATION.MONOCHROMATIC_IMAGE_AMBIENT] == null]]>
                </Expression>
            </Expressions>
            <Compare expression="monochromatic_ambientImage">
                <PartImage x="${ICON_X}" y="${ICON_Y}" width="${ICON_SIZE}" height="${ICON_SIZE}" tintColor="${AMBIENT_COLOR}" alpha="0">
                    <Variant mode="AMBIENT" target="alpha" value="255" />
                    <Image resource="[COMPLICATION.MONOCHROMATIC_IMAGE_AMBIENT]" />
                </PartImage>
                <PartImage x="${ICON_X}" y="${ICON_Y}" width="${ICON_SIZE}" height="${ICON_SIZE}" tintColor="${COLOR}" alpha="255">
                    <Variant mode="AMBIENT" target="alpha" value="0" />
                    <Image resource="[COMPLICATION.MONOCHROMATIC_IMAGE]" />
                </PartImage>
            </Compare>
            <Compare expression="monochromatic_noAmbientImage">
                <PartImage x="${ICON_X}" y="${ICON_Y}" width="${ICON_SIZE}" height="${ICON_SIZE}" tintColor="${AMBIENT_COLOR}" alpha="0">
                    <Variant mode="AMBIENT" target="alpha" value="255" />
                    <Image resource="[COMPLICATION.MONOCHROMATIC_IMAGE]" />
                </PartImage>
                <PartImage x="${ICON_X}" y="${ICON_Y}" width="${ICON_SIZE}" height="${ICON_SIZE}" tintColor="${COLOR}" alpha="255">
                    <Variant mode="AMBIENT" target="alpha" value="0" />
                    <Image resource="[COMPLICATION.MONOCHROMATIC_IMAGE]" />
                </PartImage>
            </Compare>
        </Condition>
    </Complication>
</ComplicationSlot>